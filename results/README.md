# Results Directory

Generated outputs are written here by default and are ignored by Git.

Review every output carefully before sharing anything publicly. Participant-level predictions, files containing `eid`, trained model artifacts, SHAP weights, feature importances, and other UK Biobank-derived outputs should usually stay private unless explicitly approved.
